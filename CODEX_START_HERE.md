# Codex 인계 — EMBERFALL · 잿불의 성역

이 파일은 기존 게임의 개발을 다른 PC 또는 Codex 계정에서 이어가기 위한 시작점이다. 기획 제안만 하고 끝내지 말고, 기존 소스를 읽고 수정·검증·저장소 반영까지 수행한다. 새 프로젝트로 재제작하지 않는다.

## 1. 가져올 저장소와 실행

- 저장소: https://github.com/ripper025-max/-
- 작업 브랜치: main
- 공개 게임: https://emberfall-dungeons.limsingeol.chatgpt.site
- 현재 게임 v4 공개 성공 확인. GitHub의 게임 파일과 공개 소스의 게임 파일은 같은 내용이다.
- 직전 게임 구현 커밋(GitHub): 3483e078a4b19bb8d3202b3a8560e24d2995950e
- 직전 공개 소스 커밋(Sites): 37dce9d8fcef47767e3e2c3a9ee2ac1b503f1799

Codex에서 이 GitHub 저장소를 작업 대상으로 연결하거나 PC에 복제한다. 저장소 이름은 실제로 하이픈 한 글자(-)다. 기존 로컬 폴더가 있다면 새 복제본을 덮어쓰지 말고 상태부터 확인한다.

```bash
git clone https://github.com/ripper025-max/-.git emberfall
cd emberfall
npm start
```

Node.js 22 이상 필요. 기본 실행에 npm install이나 빌드가 필요 없다. 서버가 표시한 http://127.0.0.1:4173 에서 연다. Windows는 START_GAME.cmd로도 실행된다. index.html을 직접 더블클릭하지 않는다.

## 2. 먼저 읽을 파일

1. AGENTS.md: 변경 시 지켜야 할 작업 규칙.
2. 이 파일: 사용자 의도, 확정 사항, 다음 작업.
3. README.md: 구현 내용과 검증 명령.
4. DESIGN.md: 직업별 운영, 장비 시너지, 표현 기준.
5. HANDOFF.md: 저장 호환과 다른 계정/환경에서의 주의점.

## 3. 확정된 사용자 요구 — 임의로 되돌리지 말 것

- 마인크래프트 던전스 계열의 블록 형태 비주얼을 바탕으로, 디아블로 3·4 및 PoE 2처럼 직업과 장비 시너지가 뚜렷한 핵앤슬래시 RPG.
- 직업 없는 단일 캐릭터 방식은 비교 후보로 언급됐을 뿐이며 사용자가 변경 지시가 아니라고 정정했다. 이후 직업제로 진행하기로 확정했다.
- 전사·원소술사·사냥꾼·도적·강령술사·용병·드루이드·수도사 8직업. 이상한 창작 직업명보다 이해하기 쉬운 이름을 선호한다.
- 각 직업 전용 무기 종류는 정확히 하나. 전사 대검 / 원소술사 원소 지팡이 / 사냥꾼 활 / 도적 쌍검 / 강령술사 낫 / 용병 쇠뇌 / 드루이드 자연 지팡이 / 수도사 수련봉. 무기 종류를 늘리지 말고 같은 종류의 등급·옵션·전설 효과로 파밍 깊이를 만든다.
- 솔로와 같은 화면 로컬 2인. 온라인 멀티를 구현했다고 말하지 않는다.
- 개별 드롭 필수. 두 사람은 일반 몬스터부터 각각 별도로 보상을 추첨하고 전용 장비는 본인 직업에 맞춘다. 번갈아 한 명만 받는 방식으로 회귀하지 않는다. 같은 직업 둘이어도 개인별 장비다.
- 한 장의 넓은 평원 사냥터. 통로로 방을 잇거나 구역별 문을 닫는 구조가 아니다. 중간중간 드문 오브제가 있고 회피 공간과 시야를 확보한다.
- 마을 정비 → 균열 사냥 → 진행도 100% → 같은 필드에 보스 등장 → 장비 보상 → 다음 단계 준비. 15분 내 처치 시 다음 단계·파편 보너스. 시간 초과해도 장비는 지급한다.
- 플레이어 돌진과 Space 스킬은 제거된 상태를 유지한다. 적의 돌진은 예고 후 실제 이동하는 공격이다.
- 스킬은 3개 고정이 아니라 직업별 5개 중 3개 장착하는 구현까지 완료했다. 마을에서 교체한다. 총 40스킬·80룬 선택지.
- 핵심 요구는 수치만 증가하는 장비가 아니라 스킬의 작동 방식이 바뀌는 시너지. 강령술사의 소환 수 증가, 영구 유지, 다양한 소환체가 대표 예시이며 모든 직업에 적용한다.
- 장비 잠금·비교·선택 분해·성장 초기화·빌드 저장은 유지한다. 랜덤 보상 이벤트와 특별 협동 합동기는 사용자가 제외했다.
- 효과와 액션 완성도가 가장 큰 불만. 화려한 효과, 명확한 타격감, 편리하고 읽기 쉬운 인터페이스가 우선이다.
- 판매가 5만원 수준은 장기 목표, 완성도 80/100은 개선 목표다. 현재 자동 검사 통과를 근거로 목표에 도달했다고 단정하지 않는다.

## 4. 이미 구현된 빌드 예시

강령술사: 망령 소환 + 군단의 분노 룬 + 영혼 6 + 군단의 인장으로 망령 7기. 뼈 군주의 지휘봉으로 궁수 2기·골렘 1기를 추가하고 영원의 서약을 착용하면 총 10기를 시간제한 없이 유지한다. 별도 궁수 스킬로 궁수 상한 3기를 활용할 수도 있다. 종류별 상한, 장비 해제 시 유한 지속으로 복귀, 새 균열에서 초기화되는 규칙이 있다. 현재 소환체는 자체 체력이 있는 유닛이 아니라 소유자에게 귀속된 공격체다.

전사 여진/회오리, 원소술사 동결 파쇄/운석 불바다, 사냥꾼 유도 화살/긴 화살비, 도적 이중 칼날 폭풍, 용병 포탑 증가/냉각 번개, 드루이드 늑대 강화/이동 뿌리, 수도사 잔류 번개/이동하는 진이 룬과 결합한다. 전설 총 24개. 정확한 수치의 기준은 dist/arsenal.mjs와 dist/nature.mjs다.

## 5. 소스 위치

| 경로 | 역할 |
| --- | --- |
| dist/index.html, dist/style.css | 게임 화면 구조와 반응형 스타일 |
| dist/main.mjs | 입력, HUD, 장비·스킬 선택·지도·마을 UI, 효과음 |
| dist/sim.mjs | 전투, 개인 드롭, 아이템, 성장, 소환, 저장 검증 |
| dist/arsenal.mjs | 전용 무기, 직업 특성, 전설, 보스 보상 목록 |
| dist/nature.mjs | 추가 직업과 각 직업의 추가 스킬 |
| dist/progression.mjs | 룬과 옵션 재설정 |
| dist/render.mjs | 캐릭터/무기/곰 형태와 전체 Canvas 렌더링 |
| dist/skill-fx.mjs, dist/effects.mjs | 스킬·투사체·지대·소환체 효과 |
| dist/world.mjs, dist/world-render.mjs | 평원, 마을, 오브제, 지도 |
| tests/ | 전투·저장·드롭·빌드·렌더링 검사와 구버전 저장 표본 |
| tools/serve.mjs, tools/test.mjs | 독립 실행 서버와 검사 실행기 |

여기서 dist/는 폐기 가능한 빌드 결과물이 아니라 직접 편집하는 원본이다. 지우거나 일반적인 빌드 명령으로 덮어쓰지 않는다. 파일명을 src/로 바꿀 필요도 없다.

## 6. 검증 결과와 실제 한계

- 핵심·기존 저장·무기·룬·마을·균열 검증 통과.
- 40스킬 × 기본형/룬 두 종류 = 120회 시전 및 저장 복원 통과.
- 일반 개인 드롭의 네 결과(둘 다 없음/1P만/2P만/둘 다), 정예 각자 보상, 동일 직업 2인, 전용 무기 추첨 검증.
- 10기 영구 혼성 군단, 반복 소환 상한, 영구 장비 해제 처리 검증.
- 마을 스킬 변경, 중복 장착 거부, 재사용 시간 보존, 빌드 저장/복원 검증.
- 가방 초과 장비의 보관 대기함 저장·복원·수령 검증. 자동 판매하지 않는다.
- 8직업 솔로 + 3개 협동 조합, 총 11개 자동 조작 전투로 균열 완료와 다음 단계 준비 통과.
- Canvas API에서 모든 40스킬 및 혼성 소환체 그리기 실행 통과.
- 실제 브라우저 UI 조작, 물리 컨트롤러, 기기별 시각 검사, 사람의 손맛 평가와 실기 FPS 측정은 아직 하지 않았다.

```bash
npm test
npm run test:full
npm run test:render
```

render 검사는 선택 의존성 @napi-rs/canvas가 필요하다. 없는 환경에서 검사를 못 했으면 미수행으로 보고한다. 기존 검사 문구 일부에 옛 6직업/10무기라는 설명이 남아 있지만 해당 자료를 현재 설계로 해석하지 않는다.

## 7. 다음 작업 순서

1. 저장소 최신 내용과 로컬 변경을 비교하고 기존 게임을 실행해 현재 상태를 파악한다.
2. 사용자의 최우선 불만인 전투 표현과 UI부터 개선한다. 공격 준비·스윙/발사·적중·잔광·효과음의 타이밍을 맞춘다. 추가 스킬 일부는 아직 공통 효과를 사용하므로 직업별 시각 언어를 더 구분한다.
3. 2인 화면에서 소환 군단과 여러 지대가 겹쳐도 캐릭터와 적 공격 예고가 읽히게 한다. 성능을 측정하기 전 FPS나 최적화 완료를 주장하지 않는다.
4. 스킬 선택·룬·전설 시너지 정보를 더 이해하기 쉽게 정리한다. 스킬 교체 후 UI 아이콘·재사용·자원 표시와 강화 설명의 일치를 우선 확인한다.
5. 전설 획득까지 걸리는 시간, 무장비/시너지 완성 빌드의 전투 차이, 직업 간 사냥 속도를 확인하며 조정한다. 기능 수만 늘리거나 직업당 무기를 추가하지 않는다.
6. 관련 검사를 실행하고 실제 변경과 미검증 범위를 기록한다. 테스트만 반복하며 구현을 미루지 않는다.
7. 작업 완료 후 GitHub에 변경사항을 반영하고 이 파일과 HANDOFF.md를 갱신한다. 저장소 보호 규칙이 있으면 따른다.

## 8. 저장과 배포

저장 키 emberfall.save.v1과 schema version 1, 아이템 ID·능력치·옵션·골드·장비·성장 기록을 보존한다. 추가 필드는 skillChoices[3], overflow[], loadout.skillChoices. 강화 랭크는 장착 슬롯에 적용된다. 옛 무기 종류만 전용 무기로 전환한다.

캐릭터 기록은 브라우저에 저장된다. 집에서 같은 캐릭터로 하려면 기존 게임 메뉴의 기록 백업 JSON을 집 환경에서 복원한다. GitHub는 게임 소스를 공유하며 개인 플레이 저장이나 대화 전체를 자동 공유하지 않는다.

.openai/hosting.json은 원래 Sites 프로젝트의 연결이다. 다른 계정에서 공개 권한이 없어도 로컬 수정은 가능하다. 임의로 ID를 바꾸거나 대체 사이트를 만들지 않는다. GitHub 업로드와 공개 게임 배포는 별도다. 현재 인계 정리는 문서만 바꾸므로 게임 재배포는 필요 없다.

이 작업은 일반 CLI의 GitHub 쓰기 자격증명 대신 연결된 GitHub 기능으로 원자적인 커밋을 반영했다. 새 환경에서 push가 실패하면 인증 또는 브랜치 정책을 확인하고 실패를 숨기지 않는다. force-push로 원격 작업을 덮어쓰지 않는다.
