# 집의 Codex에서 이어서 작업하기

## 최신 상태 — 2026-09-11 · DREAD (v5 소스)

이 절이 아래 과거 기록보다 우선한다. 현재 표시 이름은 **DREAD · 드레드**다. 사용자 피드백으로 짧은 제목과 별도 메인 화면을 적용했다. 저장 키와 기존 아이템/프로필은 그대로 유지한다.

- 메인 메뉴 → 영웅 선택 → 마을 → 균열 입장. 저장 기록이 있으면 계속하기로 마지막 파티를 복원한다. 선택 화면에는 실제 게임 모델의 회전 미리보기가 있고 미리보기는 저장을 변경하지 않는다.
- 마을과 야외는 비전투 공간이며 몬스터가 없다. B 또는 마을 메뉴로 입장하면 별도 열린 균열 던전으로 전환한다. 통로·구역 문·순서 강제는 없다. 처치 진행도 100% → 수호자 → 개인 장비 보상 → 마을에서 다음 단계 준비.
- 귀환은 던전의 적·전리품·진행도·파티별 복귀 좌표를 유지한다. 귀환 중 취소 규칙과 기존 시간 보상은 유지한다. 마을에서 빈 구역을 완료했다고 보상을 주던 방 단위 처리도 제거했다.
- K / 상단 버튼 / 마을 훈련에서 스킬 창 접근 가능. 던전에서는 스킬 교체를 잠근다. 이전 메뉴 수정과 함께 브라우저에서 검증한다.
- 로고, 메인 배경, 석재 텍스처, 16프레임 타격 이미지 사용. 모델은 로컬 좌표의 몸·손·무기를 하나의 방향으로 회전하고 면 깊이를 정렬한다. 흉갑·투구·무릎·장갑 디테일, 소환체 분산 배치와 효과 겹침 감소 포함.
- 구현은 Canvas 기반 블록 모델이다. 완전한 3D 엔진·온라인 멀티·상용 패키지 실행파일로 전환한 것은 아니다. 실제 장기 플레이 밸런스와 기기별 FPS, 물리 컨트롤러 검증은 남아 있다.
- 공개 Sites 게임은 v4 그대로다. 소스 동기화와 공개 배포는 별도이며 이번 작업은 공개 배포하지 않았다.

추가 파일: dist/actor-model.mjs, dist/hero-preview.mjs, dist/visual-assets.mjs, dist/assets/. 이미지 제작 기록은 ART_ASSETS.md. 추가 브라우저 검사: npm run test:menu, npm run test:presentation (로컬 서버와 선택 의존성 playwright 필요; Windows Edge는 BROWSER_CHANNEL=msedge).

먼저 [CODEX_START_HERE.md](CODEX_START_HERE.md)를 읽는다. 요구사항·구현·검증·다음 작업을 한곳에 정리했다.

## 현재 상태

- EMBERFALL · 잿불의 성역, 한국어 브라우저 RPG. v4 소스는 이 저장소에 보관한다.
- 공유 저장소: https://github.com/ripper025-max/-
- 공개 게임: https://emberfall-dungeons.limsingeol.chatgpt.site
- 공개 게임은 v4이며, 아래 2026-09-11 후속 수정은 GitHub 소스에만 반영한다. GitHub 반영과 Sites 공개는 별도 작업이다.
- 최신 확정 방향과 기능은 README.md / DESIGN.md를 읽는다. 과거 6직업·10무기·4개 생태 구역 설계는 폐기됐다.

## 실행

1. 최초 한 번 GitHub 저장소를 집 PC로 복제하거나 Codex의 작업 저장소로 연결한다.
2. PC에서 `package.json`이 있는 폴더를 작업 폴더로 연다.
3. Node.js 22 이상이 준비되어 있으면 `npm start`를 실행한다. 별도 패키지 설치는 필요 없다.
4. http://127.0.0.1:4173 에서 게임을 연다. Windows에서는 `START_GAME.cmd`로 서버를 시작할 수도 있다.
5. `dist/` 안의 코드를 수정하고 브라우저를 새로고침한다. `index.html`을 더블클릭하는 방식은 ES 모듈 때문에 사용하지 않는다.

포트가 사용 중이면 `npm start -- --port 4174`로 실행하고 표시되는 새 주소를 연다. 서버 종료는 Ctrl+C다.

## 매번 압축하지 않고 작업을 주고받는 방법

작업을 마친 쪽에서 변경 내용을 GitHub에 반영하고, 다른 쪽에서는 같은 저장소의 최신 내용을 가져온 다음 수정한다. 두 환경에서 동시에 같은 파일을 편집하면 병합이 필요할 수 있다. 파일 저장만으로 실시간 동기화되는 것은 아니다.

Codex에는 다음처럼 지시하면 된다.

> 이 저장소의 AGENTS.md와 HANDOFF.md를 읽고 EMBERFALL 작업을 이어가줘. 먼저 GitHub 최신 내용과 현재 변경사항을 확인해줘. 수정 후 필요한 검증을 하고 변경사항을 같은 저장소에 반영해줘. 새 게임으로 다시 만들지 마.

다른 ChatGPT 계정이라도 해당 저장소에 접근 가능한 GitHub 계정을 연결하면 소스를 이어서 다룰 수 있다. 웹형 Codex에서 저장소 선택이 필요하면 같은 GitHub 저장소로 환경을 만든다. 기존 사이트의 배포 권한과 소스 접근 권한은 별개다.

## 구현된 기능

8직업, 직업별 전용 무기 하나, 5개 중 3개 스킬 장착, 40스킬·80룬·24전설. 두 사람의 일반/정예/보스 보상은 독립적으로 결정된다. 넘친 장비는 보관 대기함에 저장하며 자동 판매하지 않는다. 평원 균열 진행도 100% → 수호자 등장 → 시간 내 처치 보너스 → 마을 정비 구조. 플레이어 돌진/Space 스킬은 없다. 강령술사 영구 혼성 소환과 모든 직업의 룬·장비 시너지 포함.

## 파일 안내

| 파일 | 역할 |
| --- | --- |
| dist/main.mjs | 입력·HUD·장비/스킬/마을/지도 화면 |
| dist/sim.mjs | 전투·성장·저장 검증·장비·귀환 |
| dist/render.mjs | Canvas 렌더링·캐릭터/무기 동작·카메라 |
| dist/world.mjs | 필드 크기·구역·마을·충돌 오브제 |
| dist/world-render.mjs | 지형·마을 건물·오브제·지도 |
| dist/arsenal.mjs | 전용 무기·전설 시너지·보스 정의 |
| dist/nature.mjs | 드루이드·수도사와 직업별 추가 스킬 |
| dist/skill-fx.mjs | 스킬·지대·투사체·강화 효과 |
| dist/progression.mjs | 룬·옵션 재설정 |
| dist/effects.mjs | 전투 이펙트 |
| dist/style.css | 반응형 인터페이스 |

## 검증 및 제한

검증 명령과 범위는 README.md에 있다. UI 브라우저 조작, 실제 게임패드/기기, 사람의 손맛 평가는 아직 수행하지 않았다. 자동 전투 통과를 상용 완성도로 표시하지 않는다.

플레이어 저장은 각 브라우저에 있다. 집으로 성장도 가져가려면 기존 게임 설정에서 저장 백업 JSON을 내려받아 새 환경의 설정에서 복원해야 한다. GitHub에는 개인 저장이나 대화 전체가 자동으로 올라가지 않는다.

저장 키 emberfall.save.v1과 schema version 1을 유지한다. 추가 필드는 skillChoices[3], overflow[], loadout.skillChoices다. 기존 프로필·아이템 ID·능력치·옵션을 유지하며 이전 무기 종류만 직업별 전용 무기로 전환한다. 옛 영구 소환 장비를 해제할 때 소환 상태도 갱신해야 한다. 캐릭터별 스킬이 아닌 슬롯 강화 방식이므로 선택한 스킬에 슬롯 랭크가 적용된다.

원래 Sites 소스 이력과 GitHub 최초 반영 이력은 시작점이 다르다. 같은 내용의 두 이력을 로컬에서 병합해 연결한다. 원격의 새로운 사용자 작업을 덮어쓰거나 강제 푸시하지 않는다. 연결된 GitHub 도구로 반영할 경우에도 최신 원격을 읽고 현재 스냅샷을 원자적인 커밋으로 반영한다.

## 2026-09-11 전투 표현·스킬 메뉴 수정

- 지속 효과 카드에 잘못 들어간 스킬 교체 UI와 미정의 source 참조를 제거했다. 스킬 메뉴를 8직업 모두 열 수 있다.
- 룬 선택 후 HUD 이름과 접근성 이름을 함께 갱신한다. 장착 버튼에 선택 상태와 스킬 설명을 표시하며, 피해가 없는 회복·강화 스킬에 잘못 표시되던 단계 피해 증가 설명을 수정했다.
- 추가 스킬 16개의 연출을 명시적으로 연결했다. 발사 효과는 시전자, 독·번개·폭발 지대는 실제 좌표와 반경, 급소 찌르기는 실제 최근접 대상에서 표시한다. 폭발 화살 준비 고리는 0.6초 후 폭발 시점에 끝난다. 추가 스킬의 효과음도 속성에 맞췄다.
- 1P/2P 표시와 곰 변신 시간을 소환체·스킬 효과 위에 배경을 넣어 그린다. 시작 화면의 영웅 수를 8명으로 정정했다.
- Windows Node에서 core 검사와 11개 full-run, 선택 의존성 @napi-rs/canvas를 이용한 40스킬 render 검사를 통과했다. 격리된 headless Edge에서 8직업 메뉴·스킬 교체·중복 방지·룬 HUD·저장 후 재시작·2P 독립 교체를 실제 조작해 통과했다. 스킬 메뉴 스크린샷을 확인했다.
- test:ui는 실행 중인 로컬 서버와 선택 의존성 playwright 및 브라우저가 필요하다. GAME_URL로 서버 주소, BROWSER_CHANNEL=msedge로 Edge를 지정할 수 있다. CODEX_PRIMARY_RUNTIME_NODE_MODULES는 선택 패키지 경로이며 일반 실행에는 필요 없다.
- 물리 컨트롤러·사람의 손맛·실기 FPS·장기 파밍 밸런스는 미검증이다. GitHub 소스 변경과 공개 v4는 이제 다르며, 이번 작업은 공개 사이트를 배포하지 않는다. 저장 키와 스키마, 직업·무기·개인 드롭 규칙을 유지했다.

## v5 검증 결과

- node tools/test.mjs: 핵심 전투/저장/개인 드롭/균열 검사와 120회 스킬·룬 시전 통과.
- node tests/full-run.mjs: 8직업 솔로 + 3협동 조합, 총 11개 균열 완료 통과.
- 실제 Edge headless: 8직업 스킬 메뉴, 교체/룬/HUD/저장 복원, 마을 훈련 창, 균열 입장·귀환·복귀, 이미지 로딩 통과.
- 메인 메뉴: 설정·뒤로·Escape·계속하기·저장에 영향을 주지 않는 미리보기·회전 버튼·390px 화면의 로고 영역·2인 시작 통과.
- Canvas API: 40스킬·혼성 소환·지도·2단계 효과 상세 설정 통과. 데스크톱 솔로/협동 및 모바일 시작 화면을 캡처해 육안 확인.
- 실제 FPS/물리 게임패드/장시간 사람 플레이 평가는 미수행이다.

## 메인 화면 문구 정리 — 2026-09-11

사용자 요청으로 로고 아래 한글 이름과 모험 시작의 설명 문구, 메인 하단 홍보 문구를 제거했다. 로고·메뉴 여백을 정돈하고 선택 시 작은 잿불색 표시와 은은한 배경 강조를 적용했다. 저장된 영웅 정보는 계속하기 아래 유지한다.
