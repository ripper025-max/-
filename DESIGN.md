# 전투와 빌드 설계

## 최신 상태 — 2026-09-11 · DREAD (v5 소스)

이 절이 아래 과거 기록보다 우선한다. 현재 표시 이름은 **DREAD · 드레드**다. 사용자 피드백으로 짧은 제목과 별도 메인 화면을 적용했다. 저장 키와 기존 아이템/프로필은 그대로 유지한다.

- 메인 메뉴 → 영웅 선택 → 마을 → 균열 입장. 저장 기록이 있으면 계속하기로 마지막 파티를 복원한다. 선택 화면에는 실제 게임 모델의 회전 미리보기가 있고 미리보기는 저장을 변경하지 않는다.
- 마을과 야외는 비전투 공간이며 몬스터가 없다. B 또는 마을 메뉴로 입장하면 별도 열린 균열 던전으로 전환한다. 통로·구역 문·순서 강제는 없다. 처치 진행도 100% → 수호자 → 개인 장비 보상 → 마을에서 다음 단계 준비.
- 귀환은 던전의 적·전리품·진행도·파티별 복귀 좌표를 유지한다. 귀환 중 취소 규칙과 기존 시간 보상은 유지한다. 마을에서 빈 구역을 완료했다고 보상을 주던 방 단위 처리도 제거했다.
- K / 상단 버튼 / 마을 훈련에서 스킬 창 접근 가능. 던전에서는 스킬 교체를 잠근다. 이전 메뉴 수정과 함께 브라우저에서 검증한다.
- 로고, 메인 배경, 석재 텍스처, 16프레임 타격 이미지 사용. 모델은 로컬 좌표의 몸·손·무기를 하나의 방향으로 회전하고 면 깊이를 정렬한다. 흉갑·투구·무릎·장갑 디테일, 소환체 분산 배치와 효과 겹침 감소 포함.
- 구현은 Canvas 기반 블록 모델이다. 완전한 3D 엔진·온라인 멀티·상용 패키지 실행파일로 전환한 것은 아니다. 실제 장기 플레이 밸런스와 기기별 FPS, 물리 컨트롤러 검증은 남아 있다.
- 공개 Sites 게임은 v4 그대로다. 소스 동기화와 공개 배포는 별도이며 이번 작업은 공개 배포하지 않았다.

추가 파일: dist/actor-model.mjs, dist/hero-preview.mjs, dist/visual-assets.mjs, dist/assets/. 이미지 제작 기록은 ART_ASSETS.md. 추가 브라우저 검사: npm run test:menu, npm run test:presentation (로컬 서버와 선택 의존성 playwright 필요; Windows Edge는 BROWSER_CHANNEL=msedge).

## 확정된 방향

직업 기반 캐릭터를 유지한다. 마인크래프트 던전스식 직업 없는 성장으로 변경하지 않는다. 디아블로 3·4, PoE 2를 참고하되 기술명·수치·게임 규칙은 이 프로젝트에 맞춰 구현한다. 무기는 직업마다 정확히 한 종류, 액티브 슬롯은 세 개이며 다섯 후보 중 선택한다.

| 직업 | 전용 무기 | 운영과 장비 시너지 |
| --- | --- | --- |
| 전사 | 대검 | 투지 축적, 회전베기 회오리·끌어당김, 충격파 여진 |
| 원소술사 | 원소 지팡이 | 서로 다른 스킬 공명, 동결 파쇄, 다중 운석과 착탄 불바다 |
| 사냥꾼 | 활 | 정지 조준, 유도 화살, 이동하거나 집중하는 긴 화살비 |
| 도적 | 쌍검 | 연계 마무리, 잔상 참격, 이동/고정 이중 칼날 폭풍 |
| 강령술사 | 낫 | 영혼, 망령 군단 증가, 영구 유지, 궁수·골렘 혼성 군단 |
| 용병 | 쇠뇌 | 열기 관리, 포탑 수 증가, 냉각 시 포탑 시간 연장과 번개 |
| 드루이드 | 자연 지팡이 | 자연 자원, 곰 변신, 늑대 강화, 몸을 따라오는 뿌리 |
| 수도사 | 수련봉 | 연타로 기 축적, 기 소모 폭발, 잔류 번개와 이동하는 진 |

## 강령술사 예시

망령 소환 + 군단의 분노 룬 + 영혼 6 + 군단의 인장 = 망령 7기. 뼈 군주의 지휘봉으로 궁수 2기와 골렘 1기를 더하면 10기. 영원의 서약을 함께 착용하면 전부 시간제한이 사라진다. 별도 망자의 군세 스킬로 궁수를 최대 3기 유지하는 조합도 가능하다. 소환 종류별 상한이 있어 반복 시전으로 무한 증식하지 않는다. 영구 장비 해제 시 기존 소환은 12초 뒤 사라진다. 새 균열에서는 다시 소환해야 한다. 현재 소환체는 체력/사망을 가진 별도 유닛이 아니라 소유자에게 귀속된 공격체이며, 소유자가 쓰러지거나 마을에 있으면 공격을 멈춘다.

장비 네 부위에 전설을 조합한다. 전용 전설은 보스별 보상 목록에서 얻으며 게임의 사냥 게시판에 목록과 효과가 표시된다. 무조건 특정 전설을 한 판에 얻는 방식은 아니다.

## 효과 표현 기준

시전 방향과 범위를 알려주는 준비 효과 → 실제 공격 궤적 → 판정 시점의 섬광·파편·소리 → 짧은 잔광 순서. 운석은 하강 연출과 착탄 판정을 분리한다. 불바다·뿌리·칼날 폭풍·번개는 지속 피해 위치와 같은 좌표로 그린다. 곰은 몸통·발톱·보행 형태가 바뀌고, 늑대·궁수·골렘은 서로 다른 형태와 공격 방식으로 표현한다. 적의 공격 예고는 플레이어 효과에 가려지지 않도록 마지막에 다시 그린다.

## 품질 목표와 남은 검증

80점은 목표 표현이며 측정 결과가 아니다. 실제 2인 플레이로 이동·타격 지연, 화면 가림, 기본 난이도, 반복 파밍 동기와 직업간 손맛을 확인해야 한다. 실제 브라우저에서 전투가 가장 복잡한 혼성 군단·다중 지대 상황의 프레임 안정성을 측정하고, UI 가독성과 기기별 배율을 확인한다. 상용 게임 목표에는 장기 파밍 밸런스, 더 풍부한 적/보스 패턴, 아트·애니메이션·오디오의 일관성, 배포 환경별 저장/입력 검증이 더 필요하다. 이번 자동 검사만으로 이를 완료 처리하지 않는다.

## 참고 자료

- Minecraft Dungeons 공식 소개: https://www.minecraft.net/en-us/about-dungeons — 장비·마법부여와 로컬 협동 방향.
- Minecraft Dungeons II 공식 소개: https://www.minecraft.net/en-us/about-dungeons-ii — 블록 형태와 장비 액션의 후속작 방향. 실제 트레일러/이미지를 시각 검수했다는 의미는 아니다.
- Diablo IV 공식 개발 설명: https://news.blizzard.com/en-us/article/23529210/diablo-iv-quarterly-updateseptember-2020 — 액티브·패시브 성장 구분을 설명한 역사적 자료. 최신 밸런스표가 아니다.
- Diablo III 공식 2.3 패치: https://news.blizzard.com/en-us/article/19859662/patch-2-3-0-now-live — 균열 단계/진행과 제한 시간 보상에 관한 역사적 참고.
- Grinding Gear Games가 제공한 PoE 2 제품 설명: https://store.steampowered.com/app/2694490/Path_of_Exile_2/ — 액티브 스킬과 보조 효과 결합, 장비와 빌드의 상호작용. 소개된 전체 출시 계획을 현재 게임에 모두 구현된 것으로 간주하지 않는다.
